# Трек «Індексація» — стан і план (2026-09-21)

> Джерело даних: експорти GSC власника (Performance 23.08–19.09.2026; Індексування станом на 14.09.2026).
> Самі цифри — у gitignored `doc/research/` і `doc/baseline/` (правило 9); тут лише висновки й дії.

## Що показують дані

| Факт | Висновок |
|---|---|
| Приблизно половина відомих Google сторінок не проіндексована | Проблема не в ранжуванні, а в тому, що сторінки не потрапляють в індекс |
| Найбільша категорія — **«Виявлено – наразі не проіндексовано»** (більшість непроіндексованих) | Google знає URL, але не вважає за потрібне його сканувати: слабкі внутрішні сигнали, мало посилань, сайт молодий |
| Далі — «Альтернативна сторінка з канонічним тегом» і «Сторінка з переспрямуванням» | Дублі URL: сайт віддавав 200 на `/journal`, `/journal/hoverla` і `/index.html` поряд із канонічними зі слешем |
| Увесь особистий розділ (журнал, проза, Паркінсон) — **нуль показів** за 28 днів | Розділ фактично невидимий; саме він — ядро нового сайту |
| Органіку дають один пост (Джарвіс) і кейси за назвами клієнтів | Підтверджує рішення Ф1–Ф3: комерційні запити не працюють, особистий контент працює |

## Зроблено 2026-09-21
1. **Хаби, меню, breadcrumbs** (Ф2–Ф3) — кожен пост тепер досяжний за 2 кліки з будь-якої сторінки.
2. **Внутрішня перелінковка:** чотири пости мали менше трьох редакційних джерел — додано контекстні посилання;
   правило «≥3 джерела» стало enforced (`tests/internal-links.test.js`).
3. **Дублі URL прибрано:** `trailingSlash: true` і `cleanUrls: true` у `vercel.json` — неканонічні варіанти
   тепер редиректять (308) на форму зі слешем, а не віддають 200.
4. **robots.txt** — актуальні імена AI-ботів (ClaudeBot, Claude-User, Claude-SearchBot, OAI-SearchBot,
   PerplexityBot, Google-Extended); нічого не заборонено.
5. **Тонкі сторінки:** `/creative/` і сторінка кліпу дописані до осмисленого обсягу.
6. **IndexNow** — усі змінені URL подано (Bing/Yandex); GSC «Запросити індексування» — за власником.

## Що далі
- **Виміряти ефект через 2–4 тижні:** новий експорт GSC (Ефективність + Індексування) і порівняти
  к-сть проіндексованих та к-сть URL із ≥1 показом. Обсяги замалі для статистики — дивимось на напрям, не на відсотки.
- **Пости без жодного показу** — перевірити кожен на: чи є 3+ вхідні посилання (тест), чи title/description
  відповідають реальному запиту, чи є answer-first абзац на початку.
- **Не плодити нові URL**, поки наявні не проіндексовані: спершу індексація того, що є.
