# Ф1.5 — Чесні форми й узгоджені факти (коротка фаза перед Ф2)

> Частина `doc/PERSONAL_SITE_PLAN.md`. Статус: **план** (2026-09-19). Рішення власника: розбіжності — окремою фазою
> **до реалізації Ф2**; хибне повідомлення про успіх форми — функціональний баг і **найвищий пріоритет**; далі —
> узгодити ціну й прибрати або належно атрибутувати анонімні відгуки.
> Джерело правди для фактів — сторінки кейсів і `/pricing/` (правило 13 `AGENTS.md`).

## P0 — хибний «успіх» форм (функціональний баг, без рішень власника)
Звірено з кодом 2026-09-19:

| Де | Обробник | Що відбувається при збої воркера |
|---|---|---|
| `services/landing`, `services/ai`, `services/nextjs`, `services/kyiv`, `services/redesign` | inline `submitLead` | `catch (err) {}` без перевірки `res.ok` → завжди «Дякую!» **і** `generate_lead` у GA4 → заявка губиться мовчки, конверсії в аналітиці завищені |
| `/pricing/` (бриф) | inline `submitBrief` | перевіряє `resp.ok`, але в `catch` — «Show success anyway» → той самий «успіх» |
| `/` (бот) | `sendToTelegram` | «Записав! ✅» показується **до** результату; при збої — черга в `localStorage` і повтор лише при наступному візиті, відвідувач про це не знає |
| `/services/` | спільний патерн Ф1 | ✅ еталон: успіх лише після 2xx, тайм-аут 15 с, чесна помилка з контактами |
| `/` (черга повторів бота) | `flushQueue` | `queue.forEach(async …)` не очікується, а через 5 с чергу перезаписує `setTimeout` → пізній збій або паралельна заявка губляться |
| усі 7 форм | — | немає `action`/`method`/`<noscript>`: без JS браузер робить GET на ту саму адресу з іменем і контактом у URL, а заявка не йде нікуди |

**Фікс:** винести еталон `/services/` у спільний `public/js/lead-form.v1.js` (кеш `immutable`, як
`comments.v1.js`) і підключити на `/services/`, 5 лендингах і `/pricing/`: успіх і `generate_lead` — лише після 2xx;
тайм-аут 15 с; помилка — з прямими Telegram / WhatsApp / email, дані в полях зберігаються; `source` і сторінка — в
заявці. Бот головної: «Записав!» — лише після 2xx; при збої — чесно («не вдалося передати зараз, спробую ще раз,
коли повернетеся; надійніше — напишіть напряму») + наявна черга. Головну тут змінюємо свідомо: функціональний баг.
Додатково в P0 (з рев'ю Codex 2026-09-20):
- **черга бота:** послідовна обробка або `Promise.allSettled`, запис черги — лише після завершення всіх спроб;
- **без JS:** `<noscript>` ховає форму й показує прямі контакти (зроблено на `/services/`, лишилось 6 сторінок);
- **підтвердження доставки:** зараз 2xx воркера = «дійшло», хоча код воркера не в репо; додати `request_id` +
  дедуплікацію на боці воркера і відповідь виду `{delivered:true,id}`; доти текст тайм-ауту — «не вдалося
  підтвердити доставку: заявка могла надійти, продублюйте напряму»;
- **honeypot** — свідомий виняток із DoD: показує «успіх» без 2xx, щоб не підказувати боту. Ризик хибного
  спрацювання зменшено (неавтозаповнюване ім'я поля + `inert`, зроблено на `/services/`); надійніше — перевіряти
  honeypot на воркері. У DoD винести окремим рядком.

**Тести:** smoke форм узагальнити зі `smoke-services.mjs` на всі 7 сторінок (успіх / 5xx / тайм-аут / видимість /
без `generate_lead` при збої / `javaScriptEnabled: false`); policy-тест — жодна сторінка не шле у воркер поза
`lead-form.v1.js` (крім бота головної).

### ✅ P0 виконано 2026-09-20
- `public/js/lead-form.v1.js` — спільний модуль: успіх і `generate_lead` лише після 2xx, `AbortController` 15 с,
  чесний текст «Не вдалося підтвердити доставку заявки…», дані в полях зберігаються, `source` = шлях сторінки,
  події `lead:sent` / `lead:error` для сторінкової аналітики; кеш `immutable` у `vercel.json`.
- Підключено на `/services/`, 5 лендингах і `/pricing/`; inline `submitLead` / `submitBrief` видалено
  (`grep submitLead public/` → 0). `/pricing/` перевела свої GA-події на `lead:sent` / `lead:error`.
- Бот головної: `sendToProxy` з тайм-аутом, «Записав! ✅» лише після 2xx, інакше чесне «Не вдалося передати
  прямо зараз»; `flushQueue` став `async` з `Promise.all` і одним записом черги — гонку усунено.
- `<noscript>` на всіх 7 сторінках ховає форму й показує прямі контакти (на `/pricing/` контакти вставлено
  в сам `<noscript>`: інших видимих контактів на сторінці немає).
- Telegram-посилання зведено до `t.me/+380936429885` (рішення №11 за замовчуванням).
- Тести: `tests/lead-forms.test.js` (8 перевірок, усі мутанти вбито) + `scripts/smoke-forms.mjs`
  (`npm run smoke:forms`, 37/37: успіх / 5xx / без JS на кожній із 7 сторінок + специфіка `/services/`).
- **Лишається відкритим:** підтвердження доставки з боку воркера (`request_id` + `{delivered:true,id}`) —
  код воркера не в репо; honeypot свідомо показує «успіх» без 2xx.

## P1 — ціни (рішення власника)
Дві моделі одночасно:

| Модель | Де |
|---|---|
| «Стартовий 20 000 / Оптимальний 30 000 / Максимальний 40 000 ₴» (бізнес-сайт від **30 000**) | головна (+ schema `OfferCatalog` 30000), `services/kyiv` (+ FAQ, `priceRange`), `blog/chek-list-zamovlennya-sajtu`, `blog/yak-zamovyty-sajt` |
| `/pricing/`: лендинг від 20 000, візитка від 25 000, бізнес-сайт від **40 000**, магазин від 60 000, кастом / AI від 80 000 ₴ | `/pricing/` (+ schema), `/services/`, `services/nextjs`, `blog/skilky-koshtuye-sajt` (+ schema), `services/redesign` |

**Неповнота інвентаря (рев'ю Codex):** окремі прайси є ще на `services/ai` (15 / 20 / 30 тис. ₴ за чат-бот,
Telegram-бот, кастомне рішення) і `services/redesign` (25 / 40 тис. ₴) — таких категорій у `/pricing/` немає,
і в JSON-LD вони не потрапили, тож тест лише по schema їх не побачить. Рішення власника: додати ці категорії
в `/pricing/` чи прибрати локальні прайси; тест — і за видимим текстом, і за schema.

✅ **Рішення власника 2026-09-20:** модель **B** — джерело правди `/pricing/`; вирівнюємо головну, `services/kyiv`,
`blog/chek-list-zamovlennya-sajtu`, `blog/yak-zamovyty-sajt` і schema. Строк лендингу — **5–7 днів** (як у `/pricing/`).
Категорії `services/ai` (15/20/30 тис.) і `redesign` (25/40 тис.) — рішення не дано, за замовчуванням додаю їх
у `/pricing/` як окремі рядки, щоб локальні прайси мали джерело.

**Рекомендація:** джерело правди — `/pricing/` (повна методика, 6 типів, schema; на нього вже спирається `/services/`).
Після рішення — вирівняти тексти й schema на решті сторінок; строки лендингу (5–7 / 7–14 / 5–10 днів /
1–2 тижні) — так само за `/pricing/`. **Тест:** ціни в JSON-LD інших сторінок ⊆ цін `/pricing/`.

## P1 — відгуки (рішення власника щодо кожного)
| Відгук | Сторінки | Проблема |
|---|---|---|
| «Клієнт Atlas Store» | `landing`, `nextjs`, `redesign` | анонімний; **3 різні формулювання** «дослівної» цитати |
| Сергій П., «Славутич» | `pro-mene`, `kyiv`, `landing`, `nextjs`, `redesign` | ініціал; **формулювання різні** на кожній сторінці; цифри PageSpeed 40 → 95, «5 днів», «бронювання зросли» — яких немає в кейсі |
| Жанна Вихристенко, ACE | `pro-mene`, `kyiv`, `landing`, `nextjs`, `redesign` | повне ім'я є; 2 формулювання; «Performance 100 в Lighthouse» — немає в кейсі |
| «Дослівно»: Сергій П., Максим П., Сергій М. | головна | ініціали; жартівливі цитати з позначкою «дослівно» |

✅ **Рішення власника 2026-09-20:** «Клієнт Atlas Store» — **прибрати всюди**; відгуки Сергія П., Жанни
Вихристенко і жартівливий блок на головній — **лишаємо як є**.
⏳ Лишається технічне: одна «дослівна» цитата не може мати 5 різних текстів — звести кожен відгук до одного
формулювання (найповнішого), не змінюючи змісту. Цифри всередині цитат (PageSpeed 40 → 95, Performance 100)
лишаються словами клієнта; якщо власник підтвердить їх фактами — додати в кейси. **Тест:** маніфест канонічних відгуків — текст на будь-якій
сторінці = канонічний, автор — повне ім'я (без «Клієнт …» та ініціалів).

## P2 — інші розбіжності (переважно без рішень; позначені — з рішенням власника)
- «5+ проєктів» (`kyiv`) → «6+», як на `/pro-mene/` і `/services/`.
- AGENTIS на головній: «207 законів, 22 000+ зв'язків» → як у кейсі: «300+ законів, 144 000+ семантичних блоків».
- «PageSpeed 95 / було 40», «5 днів» (Славутич) і «Performance 100» (ACE) поза кейсами → **власник:** додати в кейс
  із доказом або прибрати з лендингів і `/pro-mene/`.
- «Гарантія результату… доопрацьовую безкоштовно» на лендингах vs «бізнес-показники не гарантую» на `/pricing/` →
  формулювання `/pricing/`: технічний результат гарантовано, заявки й продажі — ні.
- Julia Satyr Art: кейс «бюджет 4 000 грн» vs `/pricing/` «~25 000 ₴» у «кейсах з близькими бюджетами» → **власник**.
- `/pricing/`: успіх брифу веде на `t.me/sashko1391`, решта сайту — `t.me/+380936429885` → за замовчуванням зводжу до `+380936429885` (рішення не дано).
- ✅ `/pricing/`: «5 запитань» (власнику все одно) — «15-хвилинний бриф» прибрати.
- ✅ Досвід: скрізь «другий рік у комерційній розробці» (рішення власника 2026-09-20).
- `agentis-v2.webp` на головній і в кейсі: `width/height` 1200×675 → справжні 2858×1446.
- `blog/skilky-koshtuye-sajt`: слово «клієнтів» веде на `/projects/agentis/` — це власний продукт →
  «реальні клієнтські кейси та власний продукт AGENTIS».
- ⏳ **Маніфест тверджень — рішення не дано.** На лендингах є числа, яких немає ні в кейсах, ні в `/pricing/` —
  конверсія 3–8%, «−7% конверсії за секунду завантаження», «70% трафіку з мобільних», «3–5 заявок щодня»,
  «AI зніме 80% рутини», «окупність за 1–2 місяці», «4 проєкти з міграцією за рік». Скласти маніфест
  «сторінка: твердження → джерело (кейс / `/pricing/` / зовнішнє)»; непідтверджене прибрати.

## Порядок і коміти
1. `fix(forms): honest lead forms — shared lead-form.v1.js` (P0) + smoke усіх форм → **пуш одразу**.
2. `fix(content): one price model` — після рішення щодо цін.
3. `fix(content): verified testimonials only` — після рішень щодо відгуків.
4. `fix(content): consistency cleanup` (P2).
5. `docs: Ф1.5 статус`.
Після кожного пушу — прод = репо байт у байт; тестова заявка з кожної форми (власник, 1 раз для всіх 7).

## Definition of Done
- [ ] Жодна форма не показує успіх і не шле `generate_lead` без 2xx від воркера; тайм-аут скрізь (smoke 7 сторінок).
  **Виняток:** honeypot навмисно показує «успіх» без надсилання — зафіксовано як свідоме рішення
- [ ] Без JavaScript форми не імітують надсилання: `<noscript>` з прямими контактами (7 сторінок)
- [ ] Черга повторів бота не губить заявок при повільній відповіді чи паралельному надсиланні
- [ ] Одна модель цін на всіх сторінках і в schema (тест)
- [ ] Лише підтверджені відгуки з повним ім'ям, одне формулювання (тест)
- [ ] P2 закрито; цифри на комерційних сторінках — лише з кейсів і `/pricing/`
