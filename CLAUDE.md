# CLAUDE.md — bridge до AGENTS.md

Спільні правила проєкту для всіх AI-агентів — у `AGENTS.md` (source of truth). Імпорт:

@AGENTS.md

## Лише для Claude Code
1. Спілкування з власником — українською; код і конфіги — англійською.
2. Приватний контекст, включно з тим, що не можна класти в публічне репо, — у пам'яті проєкту; у репо його не переносити.
3. Після плану чи діфа — давати готовий промпт для Codex (`codex exec --sandbox read-only "…"`); його findings
   звіряти з кодом, хибні — відхиляти з аргументом.
4. Будь-яку зміну правил вносити в `AGENTS.md`, а не сюди (тут — лише Claude-специфічне); обидва файли — одним комітом.
